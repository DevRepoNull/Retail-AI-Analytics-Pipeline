# 🌐 app.py — داشبورد یکپارچه Retail AI

این ماژول تمام خروجی‌های پروژه را در یک **داشبورد تعاملی** Streamlit جمع‌آوری می‌کند:

- تحلیل فروش (Analysis)  
- پیش‌بینی سری زمانی (Forecasting)  
- خوشه‌بندی مشتریان (RFM & Clustering)  
- تشخیص ناهنجاری (Anomalies)  

---

# 🎯 هدف

- نمایش نتایج پروژه به صورت تعاملی  
- دانلود فایل‌ها و نمودارها  
- انتخاب بازه زمانی برای مشاهده نمودارهای Forecasting  
- بررسی شاخص‌ها و متریک‌ها به صورت کارتی  

---

# 🧭 جایگاه در Pipeline

output/
├── daily_sales.csv
├── sales_forecast.csv
├── rfm_table.csv
├── customer_clusters.csv
├── rfm_clusters_plot.png
├── anomalies.csv
└── anomaly_plot.png
↓
app.py → نمایش همه نتایج در داشبورد

---

# 📥 ورودی‌ها

تمام فایل‌های تولیدشده توسط:

- clean_data.py  
- analysis.py  
- forecasting.py  
- rfm_analysis.py  
- anomaly_detection.py  

---

# 📤 خروجی‌ها

- داشبورد Streamlit تعاملی  
- امکان دانلود CSV و مشاهده نمودارها  
- Metrics کارت‌شده برای KPI های فروش  

---

# 🧩 کتابخانه‌ها

- pandas → داده‌ها  
- numpy → محاسبات عددی  
- streamlit → UI تعاملی  
- plotly.express → نمودار تعاملی  
- os → مدیریت فایل  

---

# 🔍 توضیح بخش‌بخش کد

---

## 🖥️ Layout و Navigation

```python
st.set_page_config(page_title="Retail AI Dashboard", layout="wide")
st.title("📊 Retail AI Dashboard")
st.markdown("Interactive dashboard for Retail AI Project: Sales, Forecasting & Customer Segmentation")
section = st.sidebar.radio("Navigation", ["Overview", "Analysis", "Forecasting", "RFM & Clustering", "Anomalies"])
```

- تعریف عنوان، layout و منوی Sidebar برای ناوبری بین بخش‌ها.

---

## 📂 بارگذاری CSV

```
@st.cache_data
def load_csv(path):
    if os.path.exists(path):
        return pd.read_csv(path)
    return pd.DataFrame()
```

- cache برای سرعت بیشتر و جلوگیری از بارگذاری مکرر

- اگر فایل وجود نداشته باشد، DataFrame خالی برمی‌گرداند

---

## 📝 بخش Overview

توضیح Pipeline و تمام فایل‌های خروجی موجود

امکان دانلود CSV‌ها

---

## 📊 بخش Analysis

- نمایش تصویر Histogram و متریک‌های Linear & Random Forest

```
st.metric("Linear MAE", "18404")
```

- کارت‌های KPI برای ارائه سریع شاخص‌های مدل‌ها

---

## 📈 بخش Forecasting

- بارگذاری sales_forecast.csv

- انتخاب بازه زمانی با slider

- نمایش Metrics تعاملی (Total Sales, Predicted Sales, Average Daily Sales)

- نمودار Interactive با plotly

- امکان دانلود CSV

---

## 👥 بخش RFM & Clustering

- نمایش RFM Table و Customer Clusters

- نمودار خوشه‌ها (RFM Clusters)

- دانلود CSV‌ها

---

## 🚨 بخش Anomalies

- نمایش نمودار ناهنجاری‌ها

- جدول ناهنجاری‌ها

- دانلود CSV

---

# 🧠 نکات فنی

- ساختار ماژولار: هر بخش مستقل ولی همه در یک داشبورد قابل مشاهده

- کاربری آسان برای ارائه و تحلیل سریع داده

- همه فایل‌ها از output/ خوانده می‌شوند → pipeline اتوماتیک

---

# ✅ جمع‌بندی

app.py:

✔️ تمام بخش‌های پروژه را یکپارچه می‌کند
✔️ داشبورد تعاملی Streamlit ارائه می‌دهد
✔️ دسترسی مستقیم به CSV و نمودارها را فراهم می‌کند
✔️ مناسب ارائه دانشگاهی و عملیاتی

---