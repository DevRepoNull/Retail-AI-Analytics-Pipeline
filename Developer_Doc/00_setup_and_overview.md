# 🧠 Retail AI — Developer Setup & Execution Guide

---

## 📌 نمای کلی پروژه (این سیستم دقیقاً چه کار می‌کند؟)

این پروژه یک Pipeline تحلیلی داده‌های فروش است که به‌صورت خط تولیدی اجرا می‌شود:

```
┌─────────────┐
│ Kaggle Data │
└──────┬──────┘
       ↓
┌─────────────┐
│ fetch_data  │  ← دانلود دیتاست
└──────┬──────┘
       ↓
┌─────────────┐
│ clean_data  │  ← پاک‌سازی و تجمیع
└──────┬──────┘
       ↓
┌─────────────┐
│ analysis    │  ← EDA و مدل پایه
└──────┬──────┘
       ↓
┌─────────────┐
│ forecasting │  ← پیش‌بینی سری زمانی
└──────┬──────┘
       ↓
┌─────────────┐
│ rfm_analysis│  ← بخش‌بندی مشتری
└──────┬──────┘
       ↓
┌─────────────┐
│ anomaly_det │  ← تشخیص ناهنجاری
└──────┬──────┘
       ↓
┌─────────────┐
│ Streamlit   │  ← داشبورد وب
└─────────────┘
```

---

## 🧩 کتابخانه‌های استفاده‌شده (و چرا)

### این پروژه ترکیبی از چند لایه است:

#### 🧮 پردازش داده

- pandas → DataFrame و aggregation

- numpy → عملیات عددی

#### 🤖 یادگیری ماشین

- scikit-learn → مدل‌ها و clustering

- statsmodels → مدل‌های سری زمانی

#### 📊 مصورسازی

- matplotlib

- seaborn

- plotly (تعاملی)

#### 🌐 وب اپ

- streamlit

#### 📥 دانلود داده

- kaggle API

---

## 🐍 محیط مجازی (Virtual Environment — venv)

### محیط مجازی یعنی:

##### 📦 یک فولدر ایزوله که فقط کتابخانه‌های همین پروژه داخلش نصب می‌شوند.

###### بدون venv:

- نسخه‌ها قاطی می‌شوند

- پروژه‌های مختلف خراب می‌شوند

###### با venv:

✔️ تمیز
✔️ قابل بازتولید
✔️ صنعتی

---

## 🐧🍎 نصب روی Linux و macOS

1️⃣ نصب ابزار venv

```
sudo apt install python3-venv python3-full
```
(macOS معمولاً آماده است.)

---

2️⃣ ساخت محیط مجازی

داخل فولدر پروژه:

```
python3 -m venv venv
```

ساختار:

```
venv/
├── bin/
├── lib/
└── pyvenv.cfg
```

3️⃣ فعال‌سازی venv

bash / zsh:

```
source venv/bin/activate
```

fish shell:

```
source venv/bin/activate.fish
```

وقتی فعال شد:

```
(venv) user@machine:~/retail_ai_project$
```

---

4️⃣ نصب پکیج‌ها

روش پیشنهادی:

```
pip install pandas numpy matplotlib seaborn scikit-learn statsmodels 
streamlit kaggle plotly jupyter
```

---

## 🪟 ویندوز (خلاصه)

```
python -m venv venv
.\venv\Scripts\activate
pip install -r requirements.txt
```

---

📥 کانفیگ Kaggle API

1️⃣ گرفتن API Token

برو به : https://www.kaggle.com/me/account

روی Create New Token بزن → فایل kaggle.json دانلود می‌شود.

---

2️⃣ کپی فایل
Windows:

```
C:\Users\<USERNAME>\.kaggle\kaggle.json
```

Linux / macOS:

```
~/.kaggle/kaggle.json
```
---

3️⃣ تنظیم دسترسی (Linux / macOS)

```
chmod 600 ~/.kaggle/kaggle.json
```

---

4️⃣ تست اتصال

```
kaggle datasets list
```

###### اگر لیست آمد → آماده‌ای ✅

---

## 🌍 تنظیم با Environment Variable

Windows:

```
setx KAGGLE_USERNAME "your_username"
setx KAGGLE_API_TOKEN "your_api_key"
```

Linux / macOS:

```
export KAGGLE_USERNAME="your_username"
export KAGGLE_API_TOKEN="your_api_key"
```

---

## 📊 Streamlit چیست و چطور کار می‌کند؟

Streamlit یک فریم‌ورک Python برای ساخت داشبورد تحلیلی است.

ایده:

```
Python Script → Streamlit → Web UI
```

اجرا:

```
streamlit run app.py
```

مرورگر : 🌐 http://localhost:8501

---

### 🔁 چرخه Streamlit

```
User Action
     ↓
Python Re-run
     ↓
Update Chart
```

هر بار کاربر چیزی تغییر دهد → کل اسکریپت از بالا اجرا می‌شود.

---

### ▶️ اجرای کل Pipeline

ترتیب استاندارد:

```
python fetch_data.py
python clean_data.py
python analysis.py
python forecasting.py
python rfm_analysis.py
python anomaly_detection.py
streamlit run app.py
```

---

### 🧠 جمع‌بندی این فایل

الان داریم:
✔️ نمای کلی سیستم
✔️ نصب چندسیستمی
✔️ venv حرفه‌ای
✔️ Kaggle API
✔️ Streamlit
✔️ اجرای pipeline

---