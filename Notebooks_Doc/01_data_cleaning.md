# 🧹 01_data_cleaning.ipynb — پاکسازی داده‌های فروش

این نوت‌بوک داده‌های خام خرده‌فروشی را پاکسازی می‌کند و آماده تحلیل و پیش‌بینی می‌کند.

---

## 📌 سلول 1 — بارگذاری کتابخانه و داده

```python
import pandas as pd
import numpy as np

df = pd.read_csv("../data/raw/Online_Retail.csv", encoding='ISO-8859-1')
print(df.head())
print(df.info())
```

- pandas / numpy برای مدیریت داده و محاسبات عددی

- داده خام خوانده می‌شود

- head() → 5 رکورد اول نمایش داده می‌شود

- info() → ساختار و نوع ستون‌ها بررسی می‌شود

---

## 📌 سلول 2 — حذف رکوردهای ناقص و غیرمعتبر

```
df = df[df['CustomerID'].notnull()]
df = df[(df['Quantity'] > 0) & (df['UnitPrice'] > 0)]
print(df.shape)
```

- حذف مشتریان بدون CustomerID

- حذف تراکنش‌هایی با Quantity یا UnitPrice صفر یا منفی

- چاپ تعداد رکوردهای باقی‌مانده بعد از پاکسازی

---

## 📌 سلول 3 — تکرار بررسی (اختیاری)

- همان عملیات سلول 2 برای اطمینان از پاکسازی

- چاپ دوباره تعداد رکوردها

---

## 📌 سلول 4 — ایجاد ستون‌های جدید

```
df['TotalPrice'] = df['Quantity'] * df['UnitPrice']
df['InvoiceDate'] = pd.to_datetime(df['InvoiceDate'])
```

- TotalPrice → مبلغ کل هر تراکنش

- تبدیل InvoiceDate به datetime برای تحلیل زمانی

---

## 📌 سلول 5 — ذخیره داده پاکسازی شده

```
df.to_csv("../data/clean/retail_clean.csv", index=False)
print(df.head())
```

- ذخیره داده تمیز برای استفاده در نوت‌بوک‌های بعدی

- بررسی نهایی چند رکورد اول

---

# ✅ جمع‌بندی

data_cleaning:

- داده خام را بارگذاری می‌کند

- رکوردهای ناقص و منفی را حذف می‌کند

- ستون‌های مورد نیاز برای تحلیل و پیش‌بینی ایجاد می‌کند

- داده تمیز را برای مراحل بعدی pipeline ذخیره می‌کند